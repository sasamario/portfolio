$(function() {
	$('.slider').slick({
    dots: true,
    //slidesToShowは表示するスライドの数を指定できる
    slidesToShow:2,
    //autoplayは自動再生、autoplaySpeedは自動再生の速度を設定できる
    autoplay:true,
    autoplaySpeed:1700,
  });

});
